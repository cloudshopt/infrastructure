name: Deploy Helm to AKS (via kubeconfig)

on:
  workflow_call:
    inputs:
      prod_branch:
        type: string
        default: main

      # Infrastructure repo (charts)
      infra_repo:
        type: string
        default: cloudshopt/infrastructure
      infra_ref:
        type: string
        default: main

      # Helm
      helm_chart_path:
        type: string
        required: true
      values_prod:
        type: string
        required: true
      values_dev:
        type: string
        required: true

      # K8s routing
      namespace_prod:
        type: string
        required: true
      namespace_dev:
        type: string
        required: true
      release_prod:
        type: string
        required: true
      release_dev:
        type: string
        required: true

      # Image
      image_repository:
        type: string
        required: true
      image_tag:
        type: string
        required: true

      # Smoke test
      health_prod:
        type: string
        required: true
      health_dev:
        type: string
        required: true

      # Check if secrets should be created
      apply_k8s_secret:
        type: boolean
        default: true

      # Secret name (DB/Redis)
      k8s_secret_name:
        type: string
        default: service-secrets

    secrets:
      KUBECONFIG_B64:
        required: true

      DB_PASSWORD_PROD:
        required: false
      REDIS_PASSWORD_PROD:
        required: false
      JWT_SECRET_PROD:
        required: false
      ORDER_SERVICE_KEY_PROD:
        required: false
      STRIPE_SECRET_KEY_PROD:
        required: false
      STRIPE_WEBHOOK_SECRET_PROD:
        required: false

      DB_PASSWORD_DEV:
        required: false
      REDIS_PASSWORD_DEV:
        required: false
      JWT_SECRET_DEV:
        required: false
      ORDER_SERVICE_KEY_DEV:
        required: false
      STRIPE_SECRET_KEY_DEV:
        required: false
      STRIPE_WEBHOOK_SECRET_DEV:
        required: false


permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == inputs.prod_branch && 'production' || 'development' }}
    steps:
      - name: Checkout infrastructure repo
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.infra_repo }}
          ref: ${{ inputs.infra_ref }}
          path: infra

      - name: Decide environment (ns/release/values/health)
        id: meta
        shell: bash
        run: |
          if [ "${GITHUB_REF_NAME}" = "${{ inputs.prod_branch }}" ]; then
            echo "ns=${{ inputs.namespace_prod }}" >> $GITHUB_OUTPUT
            echo "release=${{ inputs.release_prod }}" >> $GITHUB_OUTPUT
            echo "values=${{ inputs.values_prod }}" >> $GITHUB_OUTPUT
            echo "health=${{ inputs.health_prod }}" >> $GITHUB_OUTPUT
          else
            echo "ns=${{ inputs.namespace_dev }}" >> $GITHUB_OUTPUT
            echo "release=${{ inputs.release_dev }}" >> $GITHUB_OUTPUT
            echo "values=${{ inputs.values_dev }}" >> $GITHUB_OUTPUT
            echo "health=${{ inputs.health_dev }}" >> $GITHUB_OUTPUT
          fi

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: v1.30.0

      - name: Setup Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.15.4

      - name: Configure kubeconfig
        shell: bash
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG_B64 }}" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config
          kubectl cluster-info

      - name: Apply Kubernetes Secret (DB/Redis)
        if: ${{ inputs.apply_k8s_secret && inputs.k8s_secret_name != '' }}
        shell: bash
        run: |
          NS="${{ steps.meta.outputs.ns }}"

          if [ "${GITHUB_REF_NAME}" = "${{ inputs.prod_branch }}" ]; then
            DB_PASSWORD="${{ secrets.DB_PASSWORD_PROD }}"
            REDIS_PASSWORD="${{ secrets.REDIS_PASSWORD_PROD }}"
            JWT_SECRET="${{ secrets.JWT_SECRET_PROD }}"
            ORDER_SERVICE_KEY="${{ secrets.ORDER_SERVICE_KEY_PROD }}"
            STRIPE_SECRET_KEY="${{ secrets.STRIPE_SECRET_KEY_PROD }}"
            STRIPE_WEBHOOK_SECRET="${{ secrets.STRIPE_WEBHOOK_SECRET_PROD }}"
          else
            DB_PASSWORD="${{ secrets.DB_PASSWORD_DEV }}"
            REDIS_PASSWORD="${{ secrets.REDIS_PASSWORD_DEV }}"
            JWT_SECRET="${{ secrets.JWT_SECRET_DEV }}"
            ORDER_SERVICE_KEY="${{ secrets.ORDER_SERVICE_KEY_DEV }}"
            STRIPE_SECRET_KEY="${{ secrets.STRIPE_SECRET_KEY_DEV }}"
            STRIPE_WEBHOOK_SECRET="${{ secrets.STRIPE_WEBHOOK_SECRET_DEV }}"
          fi

          kubectl -n "$NS" create secret generic "${{ inputs.k8s_secret_name }}" \
            --from-literal=DB_PASSWORD="$DB_PASSWORD" \
            --from-literal=REDIS_PASSWORD="$REDIS_PASSWORD" \
            --from-literal=JWT_SECRET="$JWT_SECRET" \
            --from-literal=ORDER_SERVICE_KEY="$ORDER_SERVICE_KEY" \
            --from-literal=STRIPE_SECRET_KEY="$STRIPE_SECRET_KEY" \
            --from-literal=STRIPE_WEBHOOK_SECRET="$STRIPE_WEBHOOK_SECRET" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Helm upgrade --install
        shell: bash
        run: |
          helm upgrade --install "${{ steps.meta.outputs.release }}" "infra/${{ inputs.helm_chart_path }}" \
            -n "${{ steps.meta.outputs.ns }}" --create-namespace \
            -f "infra/${{ steps.meta.outputs.values }}" \
            --set images.app.repository="${{ inputs.image_repository }}" \
            --set images.app.tag="${{ inputs.image_tag }}" \
            --set env.IMAGE_SHA="${{ inputs.image_tag }}" \
            --atomic --wait --timeout 10m

      - name: Rollout status
        shell: bash
        run: |
          kubectl rollout status deploy/${{ steps.meta.outputs.release }} -n "${{ steps.meta.outputs.ns }}"

      - name: Smoke test
        shell: bash
        run: |
          curl -fsS --retry 10 --retry-connrefused --retry-delay 3 "${{ steps.meta.outputs.health }}"